server:
  port: 8080

spring:
  application:
    name: drs
  
  datasource:
    url: jdbc:mysql://localhost:3306/dr_system_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Chakri@7799
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

# Custom Application Targets for DRS System
app:
  backup:
    local-storage-path: ./storage/backups
    s3-bucket-name: enterprise-dr-bucket
    retention-days: 30
