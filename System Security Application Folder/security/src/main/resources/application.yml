server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/enterprise_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: Chakri@7799
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        enable_lazy_load_no_trans: true
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: "this_is_a_super_long_and_secure_plain_text_secret_key_that_is_at_least_64_bytes_long_for_hs512_security"
  access-expiration: 3600000     
  refresh-expiration: 604800000  
