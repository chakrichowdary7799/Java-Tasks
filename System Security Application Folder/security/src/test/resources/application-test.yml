spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1 # Emulates a database instance in RAM memory
    driver-class-name: org.h2.Driver
  data:
    redis:
      repositories:
        enabled: false # Suspends live cluster requirement hooks during test routines